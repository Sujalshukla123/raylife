apiVersion: v1
kind: Service
metadata:
  name: ray-radius-service-headless
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: ray-radius-service
  ports:
  - name: hazelcast
    port: 5701