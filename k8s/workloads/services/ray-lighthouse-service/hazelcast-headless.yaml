apiVersion: v1
kind: Service
metadata:
  name: ray-lighthouse-service-headless
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: ray-lighthouse-service
  ports:
  - name: hazelcast
    port: 5701