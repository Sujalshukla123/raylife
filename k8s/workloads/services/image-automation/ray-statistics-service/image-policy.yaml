apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: ray-statistics-service
spec:
  imageRepositoryRef:
    name: ray-statistics-service
  filterTags:
    pattern: "master-[a-z0-9]+-(?P<ts>[0-9]+)"
    extract: $ts
  policy:
    numerical:
      order: asc