apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: ripple-engine-service
spec:
  imageRepositoryRef:
    name: ripple-engine-service
  filterTags:
    pattern: "master-[a-z0-9]+-(?P<ts>[0-9]+)"
    extract: $ts
  policy:
    numerical:
      order: asc