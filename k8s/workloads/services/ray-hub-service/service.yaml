apiVersion: v1
kind: Service
metadata:
  name: ray-hub-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/management/prometheus"  
    prometheus.io/port: "metrics"
  labels:
    app.kubernetes.io/part-of: ray-hub-service
spec:
  type: NodePort
  selector:
    app: ray-hub-service
  ports:
    - protocol: TCP
      port: 38099
      targetPort: 38099
      nodePort: 32099
      name: "metrics"