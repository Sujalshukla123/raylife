apiVersion: v1
kind: Service
metadata:
  name: ray-vendor-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/management/prometheus"  
    prometheus.io/port: "metrics"
  labels:
    app.kubernetes.io/part-of: ray-vendor-service
spec:
  type: NodePort
  selector:
    app: ray-vendor-service
  ports:
    - protocol: TCP
      port: 38111
      targetPort: 38111
      nodePort: 32111
      name: "metrics"