apiVersion: v1
kind: Service
metadata:
  name: ray-gateway-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/management/prometheus"  
    prometheus.io/port: "metrics"
  labels:
    app.kubernetes.io/part-of: ray-gateway-service
spec:
  type: NodePort
  selector:
    app: ray-gateway-service
  ports:
    - protocol: TCP
      port: 38080
      targetPort: 38080
      nodePort: 32080
      name: "metrics"