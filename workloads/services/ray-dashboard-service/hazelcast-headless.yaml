apiVersion: v1
kind: Service
metadata:
  name: ray-dashboard-service-headless
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: ray-dashboard-service
  ports:
  - name: hazelcast
    port: 5701