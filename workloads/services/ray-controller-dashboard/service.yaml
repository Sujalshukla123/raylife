apiVersion: v1
kind: Service
metadata:
  name: ray-controller-dashboard
spec:
  type: NodePort
  selector:
    app: ray-controller-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80