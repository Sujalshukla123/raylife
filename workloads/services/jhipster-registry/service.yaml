apiVersion: v1
kind: Service
metadata:
  name: jhipster-registry
  namespace: default
  labels:
    app: jhipster-registry
spec:
  type: ClusterIP
  ports:
    - name: jhipster-registry
      port: 8761
      targetPort: 8761
  selector:
    app: jhipster-registry
